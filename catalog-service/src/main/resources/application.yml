spring:
  application:
    name: spring-data

  datasource:
    url: jdbc:postgresql://${CATALOG_SERVICE_POSTGRES_HOST}:5432/${CATALOG_SERVICE_POSTGRES_DATABASE}
    username: ${CATALOG_SERVICE_POSTGRES_USERNAME}
    password: ${CATALOG_SERVICE_POSTGRES_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: create

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
    defer-datasource-initialization: true

  sql:
    init:
      mode: always

server:
  port: ${CATALOG_SERVICE_HTTP_PORT}
  error:
    include-message: always
    include-binding-errors: always
